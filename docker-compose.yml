version: '3.9'

services:
  blind-drop:
    build: .
    container_name: blind-drop-app
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.local
    command: npm run dev